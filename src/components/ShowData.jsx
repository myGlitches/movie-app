function ShowData({data}) {
    return (
        <div className="moviedataContainer">
            <h1>{data.Title}</h1>
            <h2>Movie pic here</h2>
        </div>
    )
}

export default ShowData
